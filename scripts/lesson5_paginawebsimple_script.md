

# Lección 5: Página de Perfil Personal (HTML + CSS)

## Intro

> ¿Qué vamos a ver en la lección número 5? Vamos a crear una página de perfil personal. Vamos a combinar la lección número 3, HTML, listas, imágenes, enlaces, con un CSS. ¿Qué vamos a ver en el CSS? Colores, bordes, sombras.
>
> Y finalmente vamos a crear una página. ¿Qué es el CSS? El CSS vendría siendo el maquillaje de lo que es una página web. Por ejemplo, tenemos una imagen de una página web sin CSS.
>
> Solamente con HTML. Lo que ya habíamos visto en la lección número 3. Es bastante aburrido, ¿no? Y en la imagen de la derecha tenemos un perfil personal con CSS. Se ve bastante llamativo.

## Desarrollo

### background-color

> Ahora veremos el background color. Que es el color del fondo de nuestra página web.
>
> Lo vemos así. Es como si cambiáramos el color de nuestras paredes del cuarto. Lo podemos cambiarlo cuando quieras en una sola línea de CSS.
>
> Tenemos dos maneras de definir nuestro color en CSS. Primero, siempre irá background-medio color. Sin embargo, nuestra primera opción es escribir el nombre del color.
>
> Por ejemplo, en este caso light blue o white. O buscar el código del color en HTML. Por ejemplo, acá tenemos una página web que nos dice los distintos colores en HTML.
>
> El código de los colores. Por ejemplo, un azul. Tenemos este código. O un poco más claro. Tenemos el siguiente código. Vamos a utilizar este código para ponerlo en nuestra página web.
>
> En el fondo de nuestra página web. Ponemos, como ya les había dicho, background-color. Igual a... Bueno, dos puntos. Y cuando podremos utilizar el código en HTML. O sea, el código de color. Siempre vamos a poner el hashtag.
>
> Ponemos el color. Y ya nos lo cambiamos. De la misma manera, en la tarjeta. Vamos a poner background-color. Pero ahora, lo vamos a escribir. White.
>
> Los dos puntos. Ah no, y el punto y coma. Aquí está nuestra tarjeta.
>
> Y por último, en el enlace. Vamos a hacer como un botón. Entonces también vamos a poner un background-color.
>
> Y en este caso vamos a buscar un color en HTML. Por ejemplo, un color medio lila. Y abajo vemos como le da color al fondo de la frase. Mi sitio web favorito.

### border-radius

> Ahora veremos el border-radius. Que significa en español, las esquinas.
>
> Pensémoslo así. El border-radius redondea las esquinas como una tarjeta de crédito. O, como vemos en este recuadro. Las esquinas están redondeadas.
>
> ¿Cómo se escribe el border-radius? El border-radius, al igual que su nombre. Se define como border-medio-radius.
>
> Dos puntos. Y el grosor del borde. El grosor que le queremos dar al borde. Vamos a pasar un ejemplo.
>
> Por ejemplo, en la tarjeta le queremos poner border-radius. Igual a 15px.
>
> Y como podemos ver acá. Las esquinas. Todas las esquinas están redondeadas.
>
> Asimismo. Queremos que la. Que en este caso es el enlace. Esté redondeado el borde. Entonces ponemos lo mismo. Border-radius.
>
> Y le vamos a poner 8px. Y. Acá también está redondeado. Y un dato extra.
>
> Es que para hacer un círculo perfecto. Ya no se utiliza píxeles. Sino el porcentaje. Vamos acá a la imagen. Y de la misma manera. Ponemos border-radius.
>
> Y en vez de poner píxeles. Ponemos 50%. El 50% siempre va a ser. Siempre nos va a dar. En CSS un círculo perfecto.

### padding

> Ahora veremos el padding. El padding es el espacio dentro de un elemento. Imaginemos que tenemos un regalo en una caja. El padding es el relleno de burbuja que hay dentro de la caja.
>
> Entre el regalo y las paredes. Más padding, más espacio para respirar. ¿Cómo se ve? Sin padding podemos ver que el texto está bastante pegado.
>
> Al borde de nuestra tarjeta. Sin embargo con un padding de 20px. Vemos que el texto. Está bastante separado del borde de nuestras tarjetas. Es decir que. El padding genera más espacio.
>
> Tenemos 3 casos. En el que podemos utilizar nuestro padding. ¿Cómo definimos el padding? Como el mismo nombre lo dice.
>
> Simplemente en CSS escribimos padding. Si solo lo colocamos así. Eso significa que en los 4 lados. Va a tener el mismo espacio. Sin embargo si colocamos. Dos espacios diferentes.
>
> O sea dos píxeles diferentes. En este caso 10px y 20px. Tenemos. Distintos espacios. Separación. Entre arriba abajo.
>
> E izquierda derecha. Por ejemplo en este. En este caso tenemos 10px. Entre el texto. Y arriba y abajo. Y 20px. Entre el texto. Y la izquierda y derecha. Vamos a ver.
>
> Un ejemplo en el CSS. Por ejemplo acá vemos que. La imagen, el círculo está muy pegado al borde.
>
> Casi por salirse. Igualmente este botón de mi sitio web favorito. Pero si le colocamos un padding. De. 35px. Vemos como. El círculo ya no está tan pegado. Al borde de la tarjeta. De igual manera.
>
> Mi sitio web favorito. Ya no está tan pegado. Al final de la tarjeta.
>
> Luego. Queremos arreglar esto. Vemos que el botón de mi sitio web favorito. Se ve como. Como está. La frase está casi saliéndose.
>
> Entonces podemos agrandar este botón. Vamos a poner un padding. De 10px. Para. Para arriba y abajo. Y para la izquierda y derecha. Le debemos tener más espacio. Entonces vamos a poner 16px. Y nuestro botón se vería así.

### margin

> Ahora veremos el margin. ¿Qué es el margin? Mucha gente confunde el padding con el margin. Sin embargo es totalmente diferente.
>
> El margin es el espacio que hay afuera del elemento. Imaginemos que el padding. Era el relleno dentro de la caja. El margin es el espacio. Que dejas en la mesa alrededor de la caja. Acá tenemos un ejemplo gráfico.
>
> ¿Cómo definimos nuestro margin? De la misma manera que el padding. Solo tenemos que escribir margin. Y colocar el espacio. Que queremos dejar. Entre. Entre el contenido. Y arriba y abajo.
>
> ¿Para qué se utiliza el autoa? Para esto vamos a utilizar nuestra pizarra. Imaginemos que esta es nuestra página web. Y tenemos en este caso. Una tarjeta. Nuestra tarjeta es esta.
>
> Y tiene un width. O sea un ancho. Vamos a ponerle 300px.
>
> Entonces. Como ya definimos nuestro margin. Para arriba y abajo. Que en este caso va a ser 30px. Entonces. ¿Qué hace el margin? El margin hace que esto. Se separe. ¿Cierto? Pero ahora que hace el auto. El auto hace que el espacio.
>
> Entre nuestra izquierda. Y la derecha. Sea el mismo. Es decir que si le ponemos auto. Como vemos en este ejemplo de acá. Que tiene auto.
>
> La tarjeta ya no va a estar centrada. Más a la izquierda. Que a la derecha. Si no va a estar en el centro. Pero para esto siempre tenemos que definir un width. O sea un ancho.
>
> Lo haremos acá. Ya hemos definido un width. Y nuestra tarjeta. Está más chiquita. Ahora un margin. Le vamos a poner 30px.
>
> Y vemos como se separó. Esto. O sea el final de nuestra página web. Con la tarjeta. Y el auto sirve para mandar recelos.

### text-align

> Ahora ¿Cómo hacemos para centrar un contenido? Para esto utilizamos el test align. Y el margin auto. Ya he explicado el margin auto. Así que lo vamos a sacar.
>
> Y vamos a explicar el test align. El test align center es como. El botón de centrar en word. Y para utilizar el test align en CSS. Simplemente escribimos test-align. Y lo centramos con la palabra center.
>
> Por ejemplo. Vemos que el texto está pegado más a la izquierda. Que a la derecha. Entonces lo vamos a centrar. Test align. Y le colocamos center.
>
> Y ahora vemos. Como nuestro texto está centrado. Como último paso.
>
> Vamos a colocar. Un width. O sea un ancho a nuestro círculo. Porque vemos que está sobresaliendo. De nuestra tarjeta. Entonces le vamos a colocar un width.
>
> O sea un ancho a nuestro círculo. En este caso vamos a ponerle 150 píxeles. Para que quede dentro de la tarjeta. Así.

### Datos extra

> Como último dos pasos. Y unos datos extra. Vamos a agregarle un shadow. A nuestra tarjeta. De la siguiente manera.
>
> Acá vemos. La sombra. Que le da como una mejor imagen. A nuestra tarjeta. Asimismo podemos quitarle el subrayado. A mi sitio web favorito.
>
> Con la siguiente función. TextDecoration. Y le colocamos none.
>
> Y ahora vemos como. Ya no tiene la línea. El subrayado.

## Cierre

> Lo lograron. Hoy aprendieron utilizar. Background, Border, Padding, Margin. Text Align. Y datos extras.
